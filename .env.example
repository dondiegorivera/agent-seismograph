# Agent Seismograph Environment Variables
# Copy to .env and fill in your API keys

# Required: Groq API key (free tier: 30 req/min)
# Get from: https://console.groq.com/keys
GROQ_API_KEY=gsk_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Required: Tavily API key (free tier: 1000 searches/month)
# Get from: https://tavily.com/
TAVILY_API_KEY=tvly-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional: Groq rate-limit tuning
# Minimum delay between Groq requests (seconds)
GROQ_MIN_REQUEST_INTERVAL=2.5
# Number of retries on 429 before giving up
GROQ_MAX_RETRIES=4
# Exponential backoff base/max seconds for 429 retries
GROQ_BACKOFF_BASE=2.0
GROQ_BACKOFF_MAX=30.0
# Prompt/context budgeting
GROQ_MAX_SOURCES=6
GROQ_MAX_SOURCE_CHARS=700
GROQ_MAX_QUERY_SUMMARY_CHARS=300
GROQ_MAX_CONTEXT_CHARS=8000
GROQ_MAX_OUTPUT_TOKENS=1200

# Optional: Cloudflare R2 for storage
# CF_ACCOUNT_ID=your_account_id
# R2_ACCESS_KEY_ID=your_access_key
# R2_SECRET_ACCESS_KEY=your_secret_key
# R2_BUCKET_NAME=agent-seismo-data
